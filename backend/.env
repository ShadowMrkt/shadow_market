# ============================================================
#       LOCAL DEVELOPMENT ENVIRONMENT VARIABLES
# ============================================================
# This file is loaded by django-environ in development mode.
# --- DO NOT COMMIT THIS FILE TO VERSION CONTROL (GIT) ---
# Add '.env' to your .gitignore file.

# --- Django Settings ---
# Use development settings
DJANGO_SETTINGS_MODULE=mymarketplace.settings.dev

# Enable Debug mode for local development (NEVER use True in production)
DEBUG=True

# Django Secret Key (Generated - Keep Secret!)
DJANGO_SECRET_KEY=xjyhic9v3tuq$qmt^w^hd-^saop^igni1tr&lf+w1y5#j^_ysj

# --- Database ---
# Using SQLite for local testing (recommended). Creates db_test.sqlite3 file.
# Ensure you don't have PostgreSQL specific code paths in tests that require it.
DATABASE_URL=sqlite:///db_test.sqlite3
# If you MUST use PostgreSQL, replace the line above with your TEST database URL:
# DATABASE_URL='postgres://shadowmarket_user:YOUR_TEST_DB_PASSWORD@127.0.0.1:5432/shadowmarket_test_db'

# --- Redis ---
# Default local Redis URLs (Assumes Redis is running on default port without a password)
REDIS_URL=redis://127.0.0.1:6379/1
CELERY_BROKER_URL=redis://127.0.0.1:6379/0

# --- Vault Configuration ---
# Using address and token from 'vault server -dev' output you provided
VAULT_ADDR=http://127.0.0.1:8200
VAULT_TOKEN=hvs.INgePvD8YH75WmHLyvf1awco

# Vault KV Engine details (defaults from settings/base.py)
# VAULT_KV_MOUNT_POINT=secret
# VAULT_SECRET_BASE_PATH=shadowmarket

# --- GPG Configuration ---
# IMPORTANT: Replace with an ACTUAL VALID path on your system where GnuPG can store keys.
#            Create this directory if it doesn't exist. Ensure correct path separators for Windows (\).
GPG_HOME=C:\\Users\\flowe\\gnupg_home_sm

# --- Other Settings (Optional - Add if needed for specific local testing) ---
# MONERO_RPC_URL=http://127.0.0.1:18081/json_rpc
# MONERO_WALLET_RPC_URL=http://127.0.0.1:18082/json_rpc
# BITCOIN_RPC_URL=http://user:pass@127.0.0.1:8332
# DJANGO_ENV=development