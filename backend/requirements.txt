# Target Environment:
# It is recommended to pin direct dependencies for reproducible environments.
# Use tools like pip-tools (pip-compile) to manage pinned versions and their transitive dependencies.

# --- Core Framework ---
Django==5.2.1
djangorestframework==3.16.0
django-environ==0.12.0
gunicorn==23.0.0

# --- Database & Cache ---
psycopg2-binary==2.9.10
redis==6.0.0
django-redis==5.4.0

# --- Background Tasks ---
celery==5.5.2
django-celery-results==2.6.0
drf-nested-routers==0.94.1

# --- Authentication & Security ---
django-axes==8.0.0
django-otp==1.6.0
django-simple-captcha==0.6.2
python-gnupg==0.5.4
PyNaCl==1.5.0
bleach==6.2.0
hvac==2.3.0
webauthn==2.5.2
djangorestframework-simplejwt==5.5.0
python-redis-lock==4.0.0      # <<< VERSION NEEDED >>> # Note: Was in original, but not in global_requirements.txt freeze? Add version if needed.
argon2-cffi ==23.1.0

# --- API & Utilities ---
requests==2.32.3
python-json-logger==3.3.0    # NOTE: Address internal deprecation warning via code changes in logging config.
django-filter==25.1
websockets==15.0.1           # NOTE: Address legacy usage warning via code changes where websockets are used.
sentry-sdk==2.27.0                 # <<< VERSION NEEDED >>>

# --- Cryptocurrency Libraries ---
web3==7.10.0                         # 	
monero==1.1.1                 # <<< VERSION NEEDED >>>
python-monerorpc==0.6.2             # <<< VERSION NEEDED >>>
python-bitcoinlib==0.12.2           # <<< VERSION NEEDED >>> # Used by bitcoin_service.py
bitcoinaddress==0.1.5      # <<< ADDED VERSION (Used by store/validators.py) >>>
hdwallet==3.4.0


# --- Image Handling ---
Pillow==11.0.0

# --- Core Testing Tools ---
# It's often better to keep testing tools in a separate requirements-dev.txt
django-solo==2.4.0                # <<< VERSION NEEDED >>>
pytest==8.3.5
pytest-django==4.11.1
pytest-cov==6.1.1                # Listed above
django-debug-toolbar==5.1.0         # <<< VERSION NEEDED >>>


# --- Other Development/Testing Tools (Optional - Keep commented or move to requirements-dev.txt) ---
# bandit
# safety
# pip-audit
# mypy
# flake8
# black
# isort
# icontract

# NOTE: For a fully reproducible environment, generate a lock file (e.g., requirements.lock)
# using pip-compile requirements.txt > requirements.lock and install using pip install -r requirements.lock