# shadow_market/kubernetes/namespace.yaml
# Revision History:
# 2025-04-07: Initial creation. Defines the Kubernetes namespace.

apiVersion: v1
kind: Namespace
metadata:
  # Define the name for your application's namespace
  name: shadow-market
  # Optional: Add labels or annotations if needed for your environment
  # labels:
  #   app.kubernetes.io/name: shadow-market
  #   environment: production